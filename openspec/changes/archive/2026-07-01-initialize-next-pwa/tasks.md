## 1. Project Scaffold

- [x] 1.1 Initialize the Next.js App Router project with TypeScript, Tailwind CSS, ESLint, npm, `src/`, and `@/*` alias.
- [x] 1.2 Preserve existing repository files that are not generated by the scaffold, including `stack.md` and OpenSpec artifacts.
- [x] 1.3 Verify generated npm scripts and configuration files are present and consistent with the selected stack.

## 2. App Shell

- [x] 2.1 Replace the default starter page with a minimal Flashcards hello-world landing page at `/`.
- [x] 2.2 Keep the page mobile-first and simple, without adding external UI libraries or app features.
- [x] 2.3 Update app metadata so the browser title and description identify the Flashcards app.

## 3. PWA Installability

- [x] 3.1 Add a web app manifest for `Flashcards` with `name`, `short_name`, `start_url`, `display: standalone`, and theme/background colors.
- [x] 3.2 Generate placeholder PNG icons at 192x192 and 512x512 under `public/`.
- [x] 3.3 Reference the icon assets from the manifest.
- [x] 3.4 Confirm no Service Worker, Serwist integration, or custom caching behavior is introduced.

## 4. Verification

- [x] 4.1 Run the project lint command successfully.
- [x] 4.2 Run a production build successfully.
- [x] 4.3 Verify the root route serves the Flashcards hello-world page.
- [x] 4.4 Verify the manifest is available and references existing 192x192 and 512x512 icons.
